import { StyledFooter } from "./styles/Footer.styles";

const Footer = () => {
    return (
        <StyledFooter>
            <div>
                <p>No tiene pedido?</p>
                <span>click acá</span>
            </div>
            <div>
                <p>No tiene alta?</p>
                <span>click acá</span>
            </div>
        </StyledFooter>
    );
};

export default Footer;
